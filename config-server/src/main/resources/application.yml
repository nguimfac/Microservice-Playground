spring:
  application:
    name: config-server
  profiles:
    active: 
      - native

# CONFIG SERVER PROPERTIES
  cloud:
    config:
      server:
        native:
          search-locations:
          - classpath:/configurations
server:
  port: 8888